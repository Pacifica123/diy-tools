# Пример проверки

Создайте рядом временную папку с `.mkv` файлами и сначала выполните:

```bash
python ../src/cli.py ./video --dry-run --report report.json
```

Smoke-тест не хранит медиафайлы в репозитории и проверяет только dry-run контракт.
